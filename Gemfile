source :rubygems

gem 'coffee-script'
gem 'sass'

gem 'guard'
gem 'guard-bundler'
gem 'guard-compass'
gem 'guard-coffeescript'

if /darwin/ =~ RUBY_VERSION
    gem 'growl'
    gem 'growl_notify'
else
    gem 'libnotify'
end

gem 'compass'
